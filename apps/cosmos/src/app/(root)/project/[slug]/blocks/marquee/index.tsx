import type { ProjectBlock } from '@types'

interface MarqueeBlockProps {
  marquee: ProjectBlock.MarqueeBlock
}

export const MarqueeBlock: React.FC<MarqueeBlockProps> = ({ marquee }) => {
  return <></>
}
