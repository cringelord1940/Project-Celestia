import type { PostBlock } from '@types'
import { POST } from '@/enums'

interface ReferenceBlockProps {
  reference: PostBlock.ReferenceBlock
}

export const QuoteBlock: React.FC<ReferenceBlockProps> = ({ reference }) => {
  return <></>
}
