export const metadata = {
  title: {
    template: '%s | TheIceJi',
  },
}

function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

export default Layout
